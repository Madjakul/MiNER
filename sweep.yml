# sweep.yml

program: ./optimize_miner.py
method: bayes
project: miner

metric:
  name: val_loss
  goal: minimize

parameters:
  dropout:
    min: 0.1
    max: 0.5
  lr:
    values: [0.05, 0.005, 0.0005, 0.00005]
  # momentum:
  #   min: 0.1
  #   max: 0.9
  # clip:
  #   min: 1.0
  #   max: 7.0
